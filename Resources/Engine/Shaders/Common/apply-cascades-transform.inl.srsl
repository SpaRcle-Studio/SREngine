#ifdef CASCADES_INSTANCING
    OUT_POSITION = CASCADE_LIGHT_SPACE_MATRICES[gl_InstanceIndex] * vec4(VERTEX, 1.0);
    gl_Layer = gl_InstanceIndex;
#else
    OUT_POSITION = CASCADE_LIGHT_SPACE_MATRICES[PC_SHADOW_CASCADE_INDEX] * vec4(VERTEX, 1.0);
#endif