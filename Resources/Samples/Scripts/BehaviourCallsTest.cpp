//
// Created by Monika on 12.04.2025.
//

/*#include <CppBehaviour.h>

namespace Samples {
    class BehaviourCallsTest : public CppBehaviour {
        SR_SCRIPT_BEHAVIOUR_CLASS()
    public:
        void Start() override {
            //ScriptHandle handle;
            //SharedPtr<SpaRcle::Utils::SceneObject> m = handle;
            //m->GetScriptHandle();
            std::cerr << "BehaviourCallsTest::Start()" << std::endl;
            std::cerr << "SceneObject: " << sceneObject->GetName().c_str() << std::endl;
        }

        void Awake() override {
            std::cerr << "BehaviourCallsTest::Awake()" << std::endl;
        }

        void OnEnable() override {
            std::cerr << "BehaviourCallsTest::OnEnable()" << std::endl;
        }

        void OnDisable() override {
            std::cerr << "BehaviourCallsTest::OnDisable()" << std::endl;
        }

        void OnAttached() override {
            std::cerr << "BehaviourCallsTest::OnAttached()" << std::endl;
        }

        void OnDetached() override {
            std::cerr << "BehaviourCallsTest::OnDetached()" << std::endl;
        }

        void OnDestroy() override {
            std::cerr << "BehaviourCallsTest::OnDestroy()" << std::endl;
        }

        void FixedUpdate() override {

        }

        void Update(float_t dt) override {

        }
    };
}*/